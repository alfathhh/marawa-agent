const sections=[...document.querySelectorAll('main>section')];document.querySelector('nav').addEventListener('click',e=>{const id=e.target.dataset.view;if(!id)return;sections.forEach(s=>s.hidden=s.id!==id);document.querySelectorAll('nav button').forEach(b=>b.setAttribute('aria-current',b===e.target?'page':'false'))});async function api(path,options={}){const r=await fetch(path,{credentials:'same-origin',...options});if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}document.querySelector('#check-wa').addEventListener('click',async()=>{const out=document.querySelector('#wa-state');out.textContent='Memeriksa…';try{const x=await api('/dashboard/api/ops/evolution/status');out.textContent=`WhatsApp: ${x.state}`}catch{out.textContent='WhatsApp tidak tersedia'}});document.querySelector('#pair-wa').addEventListener('click',async()=>{const out=document.querySelector('#qr');out.textContent='Meminta QR…';try{const x=await api('/dashboard/api/ops/evolution/qr',{method:'POST'});out.replaceChildren();if(x.qr){const img=document.createElement('img');img.src=x.qr;img.alt='QR pairing WhatsApp';out.append(img)}else out.textContent=x.state==='connected'?'Sudah terhubung':'QR belum tersedia'}catch{out.textContent='QR tidak tersedia'}});
